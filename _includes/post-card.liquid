<li>
  <a href="{{ post.url | relative_url }}">{{ post.title | escape }}</a>
  <time datetime="{{ post.display_date | default: post.date | date_to_xmlschema }}">{{ post.display_date | default: post.date | date: '%b %Y' }}</time>
</li>
